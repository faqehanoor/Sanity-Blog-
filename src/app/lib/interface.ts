export interface blog{
    title: string,
    overview: string,
    content: any,
    id: string,
    slug: [
        current: string,
    ];
    _createdAt: string,
    mainImage: string,
}

export interface Offer{
    title: string,
    overview: string,
    content: any,
    id: string,
    slug: [
        current: string,
    ];
    _createdAt: string,
    mainImage: string,
}

export interface contactus{
    title: string,
    overview: string,
    content: any,
    id: string,
    slug: [
        current: string,
    ];
    _createdAt: string,
    mainImage: string,
}